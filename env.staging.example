# Staging Environment Configuration
# Copy this file to .env.staging.local and fill in your staging Firebase project values

# Set environment to staging
NEXT_PUBLIC_ENVIRONMENT=staging

# Staging Firebase Configuration
# Get these values from your Firebase Console → Project Settings → General → Your apps
NEXT_PUBLIC_FIREBASE_STAGING_API_KEY=your_staging_api_key
NEXT_PUBLIC_FIREBASE_STAGING_AUTH_DOMAIN=your-staging-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STAGING_PROJECT_ID=your-staging-project-id
NEXT_PUBLIC_FIREBASE_STAGING_STORAGE_BUCKET=your-staging-project.appspot.com
NEXT_PUBLIC_FIREBASE_STAGING_MESSAGING_SENDER_ID=your_staging_sender_id
NEXT_PUBLIC_FIREBASE_STAGING_APP_ID=your_staging_app_id

# Staging Firebase Admin SDK Configuration
# Option 1: Service account key as JSON string (recommended for deployment)
# Get this from Firebase Console → Project Settings → Service Accounts → Generate new private key
# FIREBASE_SERVICE_ACCOUNT_KEY_STAGING='{"type":"service_account","project_id":"your-staging-project-id",...}'

# Option 2: Path to service account JSON file (recommended for local development)
# Download the service account key and save it as firebase-service-account-staging.json
GOOGLE_APPLICATION_CREDENTIALS_STAGING=./firebase-service-account-staging.json

# Production Firebase Configuration (fallback if staging vars not set)
# Keep these for reference, but staging vars take precedence when NEXT_PUBLIC_ENVIRONMENT=staging
NEXT_PUBLIC_FIREBASE_API_KEY=your_production_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-production-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-production-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-production-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_production_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_production_app_id

# Other Services (use staging/test credentials for staging environment)
# Resend Email Service
RESEND_API_KEY=your_resend_api_key

# Twilio SMS & WhatsApp
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Sentry Error Tracking (optional)
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project
SENTRY_AUTH_TOKEN=your_sentry_auth_token

